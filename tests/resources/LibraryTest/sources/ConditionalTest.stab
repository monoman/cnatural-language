#define DEBUG

public class ConditionalTest {
    public static bool test() {
        try {
            ConditionalAux.assert(false);
        } catch {
            return true;
        }
        return false;
    }
}